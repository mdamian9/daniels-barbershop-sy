import React from "react";
import "./Footer.css"

const footer = (props) => {
    return (
        <footer className="footer-component text-center">
            <p>
                Footer
            </p>
        </footer>
    );
};

export default footer;